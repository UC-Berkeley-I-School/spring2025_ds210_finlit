# Core Streamlit Framework
streamlit==1.43.0  # Stable version for building the frontend

# HTTP Requests for backend API calls
requests==2.31.0

# AWS SDK for Python (if your app interacts with AWS services)
boto3==1.34.28
botocore==1.34.28

# Environment Variables (for local development; consider removing in production if not used)
python-dotenv==1.0.1